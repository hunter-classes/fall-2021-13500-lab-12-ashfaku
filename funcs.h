#pragma once
#include <vector>
using std::vector;
vector<int> makeVector(int n);
vector<int> goodVibes(const vector<int> &v);
void gogeta(vector<int> &goku, vector<int> &vegeta); // vegito they should've meant
